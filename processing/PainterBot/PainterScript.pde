class PainterScript {
  
  int width;
  int height;
  
  PainterScript(String url) {
    init(url);
  }
  
  void init(String url) {
    load(url);
    parse();
  }
  
  void load(String url) {
  }
  
  void parse() {
  }
  
}
